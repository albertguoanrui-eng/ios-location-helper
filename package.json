{
  "name": "shadowrocket-location-helper",
  "version": "0.1.1",
  "private": true,
  "description": "Shadowrocket 本地坐标控制与 WLOC 诊断",
  "license": "AGPL-3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "build:github": "node scripts/package-github.mjs"
  }
}
